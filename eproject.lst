;; -*- mode: Lisp; -*-

(setq prj-version nil)

(setq prj-list
      '(("eproject" "/home/devil/.emacs.d/elpa/eproject-0.4")))

(setq prj-last-open nil)

(setq prj-frame-pos
      '((top . 32)
        (left . 0)
        (width . 80)
        (height . 33)))

